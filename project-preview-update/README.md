# Stable Project List Preview Update

Replace only `src/components/sections/Projects.tsx`.

- Hover preview is fixed on the right side of the screen; it no longer follows the cursor.
- Preview size is `30rem` wide by `34rem` high.
